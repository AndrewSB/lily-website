const device = {}
device.small = '@media (max-width: 850px)'

const color = {}
color.facebookBlue = '#1787FB'

const styles = {}
styles.flexAndCenter = {
  display: 'flex',
  alignItems: 'center'
}

export default {
  device,
  color,
  styles,
}
